{% include 'base.html.twig' %}

<body>
	<div class="container mt-5">
		<h1>Veterinarian</h1>

		<table class="table">
			<table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ veterinarian.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ veterinarian.name }}</td>
            </tr>
            <tr>
                <th>Crmv</th>
                <td>{{ veterinarian.crmv }}</td>
            </tr>
        </tbody>
    </table>

		<div class="container mb-3">
			<a href="{{ path('app_veterinarian_index') }}" class="btn btn-primary">Back to list</a>

			<a href="{{ path('app_veterinarian_edit', {'id': veterinarian.id}) }}" class="btn btn-secondary">Edit</a>

			<div style="margin-top: 20px;">
				{{ include('veterinarian/_delete_form.html.twig') }}
			</div>

		</div>
	</div>
</body>
