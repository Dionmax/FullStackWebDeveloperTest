{% include 'base.html.twig' %}

<body>
	<div class="container mt-5">
		<h1>Farm</h1>

		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ farm.id }}</td>
				</tr>
				<tr>
					<th>Name</th>
					<td>{{ farm.name }}</td>
				</tr>
				<tr>
					<th>Size</th>
					<td>{{ farm.size }}</td>
				</tr>
				<tr>
					<th>Manager</th>
					<td>{{ farm.manager }}</td>
				</tr>
			</tbody>
		</table>

		<div class="container mb-3">
			<a href="{{ path('app_farm_index') }}" class="btn btn-primary">Back to list</a>

			<a href="{{ path('app_farm_edit', {'id': farm.id}) }}" class="btn btn-secondary">Edit</a>

			<div style="margin-top: 20px;">
				{{ include('farm/_delete_form.html.twig') }}
			</div>

		</div>
	</div>
</body>
